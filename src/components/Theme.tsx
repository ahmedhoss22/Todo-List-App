import { createTheme } from "@mui/material";

    const  theme = createTheme({
      palette: {
        primary: {
          main: '#ef6c00',
        },
        secondary: {
          main: '#ff5722',
        },
      },
    });

export default theme;